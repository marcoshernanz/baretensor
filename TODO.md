mdspan
metadata-only reshape
-1 in reshape
view
autograd.cpp docs
requires_grad_ in bindings?
In softmax why return exp() on scalar Tensor? Shouldn't it be 1? Same in log_softmax
File structure - having both .c and .h or only one?
Naming conventions - requires_grad and requires_grad_ vs grad_fn and set_grad_fn
Exhaustive tests?
CMakeLists automatic (nanobind docs)?